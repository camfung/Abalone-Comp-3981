classDiagram
direction RL
namespace Players {

    class Player {
        <<abstract>>
        - game_state
        +update() : void
        +makeMove(gameManager : GameManager, player: Player, move: Move) : void
    }

    class HumanPlayer {

    }

    class AbaloneAgent {
        - generate_move()
    }
} 
    Player --> Move : Makes
    HumanPlayer --|> Player
    AbaloneAgent --|> Player
namespace driver {
    class App {
       - game : GameManager 
    }
}
namespace API {
        class Move{
        <<abstract>>
        - move_state
        - player
    }
class Observer {
        <<interface>>
        +update() : void
    }

}

namespace GameLogic {
    class Observable {
        <<abstract>>
        - game_state : Game
        - players : List[Observer]
        + join_room(roomId, player) : bool
        +leave_room(observer : Observer) : void
        +notify() : void
    }

    class Game {
        - gameState : GameState
        - recordHistory : RecordHistory
        - StartingFormatoin : Formation
        +makeMove(player: Player, move: Move) : void
        +getGameState() : GameState
    }

    class GameManger {
        - players : List[Observer]
        - roomId
        + join_game(roomId, player) : bool
        + leave_game(observer : Observer) : void
        + notify() : void
        + makeMove(player: Player, move: Move)
    }

    class GameState {
        - board : list[list[int]]
        + get_state_space(): Move[]
        + calc_white_move(): Move
        + calc_black_move(): Move
        + make_move(player, move)
    }
    class RecordHistory {
        - game_counter: int
        - records: Record[]
        + get_game_id(): int
        + get_records(): Record[]
        + get_record(index: int): Record
    }

    class Record {
        - index: int
        - game_id: int
        - move: Move
        + __init__(): Record
        + get_index(): int
        + get_game_id: int
        + get_move(): String
    }
    class GameConfiguration {
        + initialize_board_layout(): void
        + select_player_color(): void
        + set_move_limit(): void
        + set_time_limit(): void
    }
    class Formation {
        <<enumeration>>
        DEFAULT
        BELGIAN_DAISY
        GERMAN_DAISY
    }

}
App --* GameManger : has a
App --* Player : has 2
Game --* Formation : has a
Game --* RecordHistory : has a
Game --* GameState : has a
Game --> Move : uses
Game --o GameConfiguration : has a 
GameManger --|> Observable : is a
Observable --o Observer : has many
Observable --* Game : has a
Observer --> Observable : observes
Observer <|-- Player : implements
RecordHistory --* Record : has many
Record --o Move : has a 
